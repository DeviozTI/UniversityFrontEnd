import { FilterDataTablePipe } from './filter-data-table.pipe';

describe('FilterDataTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataTablePipe();
    expect(pipe).toBeTruthy();
  });
});
